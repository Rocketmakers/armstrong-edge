import { ArgTypes, Canvas, Controls, Meta, Story } from '@storybook/blocks';
import { Input } from "./input.component";
import * as InputStories from "./input.stories";
import { NumberInput } from "./numberInput";
import * as NumberInputStories from './numberInput/numberInput.stories';
import { PasswordInput } from "./passwordInput";
import { TelInput } from './telInput';
import { TextInput } from './textInput';

<Meta of={InputStories} />

# Inputs

Various flavours of wrapper for the native `<input />` element, including typing for the binds and a validation layer.

The generic `<Input />` is available for creating your own custom inputs - see [Custom Form Inputs](/docs/custom-form-inputs) for details.


