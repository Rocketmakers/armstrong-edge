import { Canvas, ArgTypes } from '@storybook/blocks';
import { Meta } from '@storybook/addon-docs/blocks';
import { ProgressBar } from './ProgressBar.component';

import * as ProgressBarStories from './progressBar.stories';

<Meta title="Components/ProgressBar" component={ProgressBar} />

# Progress bar

The progress bar can be used to visually represent progress or indicate next steps in your interface.

<Canvas of={ProgressBarStories.Default} layout="fullscreen" sourceState="none" />

```tsx
import { ProgressBar } from '@rocketmakers/armstrong';

<ProgressBar progress="40" />;
```

## Progress bar props

<ArgTypes of={ProgressBar} />
