{
  "name": "@rocketmakers/armstrong-edge",
  "version": "0.0.1",
  "description": "Initial stating project for the 2021 Armstrong rewrite. Will be integrated into the Armstrong repo.",
  "main": "index.js",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "start-playground": "(cd playground && npm start)",
    "start": "npm-run-all start-playground watch-link-module",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "(cd module && npm i) && (cd playground && npm i) && npm run link-module",
    "link-module": "(cd module && npm run build && npm run link) && (cd playground && npm run link-module)",
    "watch-link-module": "watch 'npm run link-module' ./module/src",
    "check-versions": "ncu && (cd module && ncu) && (cd playground && ncu)",
    "upgrade-all": "ncu -u && (cd module && ncu -u) && (cd playground && ncu -u) && npm i"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Rocketmakers/armstrong-rewrite.git"
  },
  "author": "developers@rocketmakers.com",
  "license": "MIT",
  "devDependencies": {
    "commitizen": "^4.2.4",
    "cz-customizable": "^6.3.0",
    "husky": "4.2.5",
    "npm-check-updates": "^11.5.10",
    "npm-run-all": "^4.1.5",
    "watch": "^1.0.2"
  },
  "husky": {
    "hooks": {
      "commit-msg": "exec < /dev/tty && git cz --hook || true"
    }
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-customizable"
    },
    "cz-customizable": {
      "config": "./commitizen.config.js"
    }
  }
}
