{
  "name": "@rocketmakers/armstrong-root",
  "version": "3.0.1",
  "description": "Rocketmakers React component library",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/Rocketmakers/armstrong-edge.git"
  },
  "author": "developers@rocketmakers.com",
  "license": "MIT",
  "scripts": {
    "setup": "asdf install && npm i && (cd module && npm i) && (cd storybook && npm i)",
    "test": "(cd module && npm test)",
    "prepare": "husky install",
    "build-storybook": "(cd storybook && npm run build-storybook)",
    "storybook": "(cd storybook && npm run storybook)",
    "test-storybook": "(cd storybook && npm run test-storybook)"
  },
  "devDependencies": {
    "commitizen": "4.3.0",
    "cz-customizable": "7.0.0",
    "husky": "8.0.3",
    "watch": "1.0.2"
  },
  "config": {
    "cz-customizable": {
      "config": "./commitizen.config.js"
    },
    "commitizen": {
      "path": "./node_modules/cz-customizable"
    }
  }
}
